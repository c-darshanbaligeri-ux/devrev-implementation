# AI Agent Guides (local cache)

Article bodies and `INDEX.md` are **generated** when you run `/agent-sync` (or `scripts/sync-knowledge.sh`). They are not committed to the marketplace plugin.

After syncing, open `knowledge/INDEX.md` for the article list and timestamps.
